node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
node_modules/.tmp

# Local env / secrets (Google Fonts API key for the catalog refresh script)
.env
.env.local

# Local tooling / assistant artifacts — never commit
.claude/
docs/superpowers/
